steps:
  - name: gcr.io/cloud-builders/git
    entrypoint: /bin/bash
    args: ["-c", "git rev-list --count ${SHORT_SHA}"]

  - name: gcr.io/cloud-builders/git
    entrypoint: /bin/bash
    args: ["-c", "git describe --tags --exact-match"]

timeout: "1600s"
